# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages

matrix:
    include:
        - os: linux
          sudo: required
        - os: osx
          language: r
          osx_image: xcode8.3
